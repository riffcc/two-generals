% Chapter 4: The Bilateral Construction Property
% Two Generals Protocol Paper (v2)

The core theoretical contribution is the \emph{bilateral construction property}: the triple proofs $\Triple{A}$ and $\Triple{B}$ form a cryptographic knot where neither can exist without the other being constructible.

\begin{theorem}[Bilateral Constructibility]
\label{thm:bilateral}
If party $A$ can construct $\Triple{A}$, then party $B$ can construct $\Triple{B}$, and vice versa:
\[
\exists \Triple{A} \Leftrightarrow \exists \Triple{B}
\]
\end{theorem}

\begin{proof}
We prove the forward direction; the reverse is symmetric.

Suppose Alice can construct $\Triple{A} = \Sign{A}{\Double{A} \| \Double{B}}$.

\textbf{Step 1:} Alice has $\Double{B}$. By definition, $\Double{B} = \Sign{B}{\Com{B} \| \Com{A}}$, so Bob received Alice's commitment.

\textbf{Step 2:} For Alice to have $\Double{B}$, Bob must have constructed it, meaning Bob had $\Com{A}$.

\textbf{Step 3:} Since Bob has $\Com{A}$, Bob can construct $\Double{B}$. And since Alice is flooding $\Double{A}$, Bob will receive it under fair-lossy conditions.

\textbf{Step 4:} Upon receiving $\Double{A}$, Bob can construct $\Triple{B} = \Sign{B}{\Double{B} \| \Double{A}}$.

Therefore, if $\Triple{A}$ exists, $\Triple{B}$ is constructible under fair-lossy conditions.
\end{proof}

\subsection{Spatial Completeness, Not Temporal Sequence}

The standard protocol builds coordination as a temporal chain: one stone, then the next, then the next ($1 \oplus 1 \oplus 1 \oplus \cdots$). Each stone is a message, and safety depends on the \emph{sequence} of arrivals. The last stone is the whole problem---if it falls into the abyss, one party believes the bridge is built while the other knows it is not. That is Gray's induction: sever the final link, and sender and receiver hold contradictory beliefs about completion.

TGP builds coordination as a spatial structure: two halves of an arch that must meet at the keystone to bear any weight ($1 \times 1$). Alice contributes one half ($\Double{A}$), Bob the other ($\Double{B}$); the attack key is forged if and only if both halves are present. If either half is absent---never sent, or sent and lost---the arch cannot stand, and \emph{neither} party crosses. The transition from idle to action depends on a \emph{spatial} requirement (the joint presence of both components) rather than a \emph{temporal} one (the arrival of a final acknowledgment in a sequence).

This is the structural reason no message is pivotal. In a chain, severing the last link leaves the sender believing completion occurred while the receiver knows it did not. In the arch, removing any stone leaves the structure incomplete for \emph{both} parties: there is no configuration of the bilateral object in which one party holds a completed key and the other does not. Completeness is a property of the joint state, not of any party's position in a delivery sequence. The braid has no end to cut.

\begin{figure}[t]
\centering
\begin{tikzpicture}[>=Stealth, font=\small,
    blk/.style={draw, rounded corners=2pt, minimum height=0.62cm, minimum width=0.72cm},
    lostblk/.style={draw, rounded corners=2pt, minimum height=0.62cm, minimum width=0.72cm, dashed, red!70!black}]

% ===== LEFT: the temporal chain =====
\node[blk] (c) at (0,0)    {$C$};
\node[blk] (d) at (1.05,0) {$D$};
\node[blk] (t) at (2.10,0) {$T$};
\node[lostblk] (q) at (3.55,0) {$Q$};
\draw[->] (c) -- (d);
\draw[->] (d) -- (t);
\draw[->, dashed, red!70!black, thick] (t) -- (q)
    node[midway, above, red!70!black, font=\footnotesize] {loss};
\draw[red!70!black, thick, dashed] (3.10,-0.55) -- (4.00,-0.55);
\node[font=\footnotesize, red!70!black] at (3.55,-0.82) {last message};
\node[font=\small, red!70!black] at (3.55,0.78) {$\boldsymbol{\times}$};
\node[font=\footnotesize, align=center] at (1.77,-1.55)
    {\textbf{Chain} $(1\oplus1)$: temporal\\ sever the last link $\Rightarrow$\\ sender and receiver disagree};

% ===== RIGHT: the spatial arch =====
\begin{scope}[xshift=7.4cm]
\node[blk] (alice) at (-1.4,-0.2) {Alice};
\node[blk] (bob)   at ( 1.4,-0.2) {Bob};
\node[blk, fill=blue!12, very thick] (key) at (0,1.6) {key};
\draw[ultra thick, blue!55!black] (alice.north) -- (key.south west);
\draw[ultra thick, blue!55!black] (bob.north)   -- (key.south east);
\node[blue!55!black, font=\footnotesize] at (-0.78,0.70) {$D_A$};
\node[blue!55!black, font=\footnotesize] at ( 0.78,0.70) {$D_B$};
\draw[gray, dashed] (-1.4,-0.72) -- (1.4,-0.72);
\node[gray, font=\footnotesize\itshape] at (0,-0.97) {lossy channel};
\node[font=\footnotesize, align=center] at (0,-1.55)
    {\textbf{Arch} $(1\times1)$: spatial\\ remove either half $\Rightarrow$\\ incomplete for \emph{both}};
\end{scope}

\end{tikzpicture}
\caption{The geometric difference. \textbf{Left:} the classical construction is a temporal chain of acknowledgments ($C \to D \to T \to \cdots$); the last message is pivotal---sever it and the two parties hold contradictory beliefs about completion. \textbf{Right:} TGP's bilateral knot is a spatial arch---two halves ($D_A$, $D_B$) meeting at a single keystone (the attack key). Removing either half leaves the structure incomplete for both parties; the keystone is forged iff both halves are present. The braid has no end to cut.}
\label{fig:chain-vs-arch}
\end{figure}

\subsection{The Cryptographic Knot}

Traditional protocols create a chain of acknowledgments where each link could be the ``last message'' that fails:
\[
\text{MSG} \rightarrow \text{ACK} \rightarrow \text{ACK-of-ACK} \rightarrow \cdots
\]

TGP creates a \emph{knot}:
\begin{center}
\begin{tikzpicture}[node distance=2cm]
    \node (TA) {$\Triple{A}$};
    \node (TB) [right=of TA, xshift=1cm] {$\Triple{B}$};
    \node (DA) [below=of TA] {$\Double{A}$};
    \node (DB) [below=of TB] {$\Double{B}$};

    \draw[<->, ultra thick, red] (TA) -- (TB) node[midway, above] {bilateral};
    \draw[->] (DA) -- (TA);
    \draw[->] (DB) -- (TA);
    \draw[->] (DA) -- (TB);
    \draw[->] (DB) -- (TB);
\end{tikzpicture}
\end{center}

$\Triple{A}$ embeds $\Double{B}$; $\Triple{B}$ embeds $\Double{A}$. Neither can exist without the other being constructible. There is no ``last message''---there is mutual cryptographic entanglement.

\subsection{Why This Breaks Gray's Attack}

Gray's impossibility relies on the ``drop last message'' attack: find an execution where one party decides ATTACK, then remove the last message to create asymmetry.

This attack fails against TGP because:

\begin{enumerate}
    \item The attack key is not triggered by receiving a message---it \emph{emerges} from state
    \item Removing $\Triple{B}$ from a schedule where Alice attacks means Alice never had $\Triple{B}$
    \item But if Alice never had $\Triple{B}$, the attack key never emerged for Alice either
    \item Therefore, Alice never attacked in the modified execution
\end{enumerate}

The bilateral construction makes the ``pivotal message'' impossible: any message whose removal would cause asymmetry cannot exist, because asymmetry requires one party to have the attack key while the other lacks it---but the knot structure prevents this.

\subsection{Formal Statement}

\begin{theorem}[No Pivotal Message]
\label{thm:no-pivotal}
For any TGP execution and any message $m$, removing $m$ cannot create an asymmetric outcome.
\end{theorem}

This is proven exhaustively in Lean 4 via \texttt{tgp\_no\_pivotal} using \texttt{native\_decide} over all 256 channel state combinations.
